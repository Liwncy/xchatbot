{
    "name": "xchatbot",
    "version": "0.1.0",
    "private": true,
    "description": "薄核：Golem 协议 + 分类型插件 + MCP / OpenClaw",
    "scripts": {
        "dev": "wrangler dev",
        "dev:scheduled": "wrangler dev --test-scheduled",
        "dev:remote": "wrangler dev --remote",
        "config:init": "node ./_docs/scripts/init-local-configs.cjs",
        "dev:pull": "npm run data -- run sync:pull && npm run dev",
        "dev:seed": "npm run data -- run kv:seed:local && npm run dev",
        "data": "node ./_docs/scripts/cloudflare-data-commands.cjs",
        "help:data": "node ./_docs/scripts/cloudflare-data-commands.cjs list",
        "typecheck": "tsc --noEmit",
        "deploy": "wrangler deploy",
        "deploy:safe": "npm run data -- run sync:pull && npm run typecheck && npm run deploy",
        "deploy:safe:push": "npm run data -- run sync:push:force && npm run typecheck && npm run deploy"
    },
    "dependencies": {
        "axios": "^1.12.2"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20250224.0",
        "@types/node": "^25.3.3",
        "typescript": "^5.8.2",
        "wrangler": "^4.70.0"
    }
}
