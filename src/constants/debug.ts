// Default debug forward switch TTL: 8 hours.
export const DEBUG_TTL_SECONDS = 8 * 60 * 60;

// Marker header to prevent forwarding loops.
export const DEBUG_FORWARDED_HEADER = 'x-xchatbot-debug-forwarded';

